[url "ssh://victor.boivie@android-seld.sonyericsson.net:29418/"]
	insteadOf = git://review.sonyericsson.net/
	insteadOf = git://android.git.kernel.org/
	insteadOf = ssh://android-seld.sonyericsson.net/

[url "ssh://victor.boivie@review.sonyericsson.net:29418/"]
        pushInsteadOf = git://review.sonyericsson.net/

[color]
	diff = auto
	status = auto
	branch = auto

[user]
	name = Victor Boivie
	email = victor.boivie@sonymobile.com
[core]
	gitproxy = semcproxy for codeaurora.org
	gitproxy = semcproxy for kernel.org
	gitproxy = semcproxy for gitorious.org
	pager = diff-so-fancy | less --tabs=1,5 -R
#	gitproxy = semcproxy for git.apache.org
	gitproxy = semcproxy for github.com

[rerere]
	enabled = 1

[alias]
	ci = commit -v
	cia = commit -v -a
	ciaa = commit -v -a --amend
	s = status
	d = diff
	dc = diff --cached
	serve = !git daemon --reuseaddr --verbose  --base-path=. --export-all ./.git
	whois = "!sh -c 'git log -i -1 --pretty=\"format:%an <%ae>\n\" --author=\"$1\"' -"
	whatis = show -s --pretty='tformat:%h (%s, %ad)' --date=short
	sortdiff = !sh -c 'git diff "$@" | grep "^[+-]" | sort --key=1.2 | uniq -u -s1'
	lol = log --pretty=oneline --abbrev-commit --graph --decorate
	fixup = !sh -c 'git commit -m \"fixup! $(git log -1 --format='\\''%s'\\'' $@)\"' -
	squash = !sh -c 'git commit -m \"squash! $(git log -1 --format='\\''%s'\\'' $@)\"' -
	ri = rebase --interactive
	rc = rebase --continue
	cp = cherry-pick
	dsf = "!git diff --color $@ | diff-so-fancy"
	prb = pull --rebase

[rebase]
	autosquash = True
[push]
	default = current

[https]
	proxy = http://localhost:3128
[credential]
	helper = osxkeychain
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
